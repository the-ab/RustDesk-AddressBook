0.6.0-ghcr-compose-version-setup-token-cleanup
[de]
- VERSION-Datei mit Version 0.6.0 ergänzt.
- Eigenen docker-compose-Ordner für die Installation über das GHCR-Image ohne Projektquellcode ergänzt.
- README, Administratorhandbuch und WebUI-Anleitung um den Image-Installationsweg erweitert.
- Einmaliges Setup-Token nach Erstellung des ersten Administrators aus config.json entfernt; bestehende Installationen werden automatisch bereinigt.
[en]
- Added the VERSION file with version 0.6.0.
- Added a dedicated docker-compose directory for source-free installation from the GHCR image.
- Extended README, Admin Guide and Web UI help with the image installation path.
- Removed the one-time setup token from config.json after the first administrator is created; existing installations are cleaned automatically.
