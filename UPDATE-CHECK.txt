rustdesk-addressbook-update-flat-v0528.zip
[de]
- Neustartschleife bestehender Installationen durch einen separaten Berechtigungs-Init-Dienst behoben; die Webanwendung bleibt unprivilegiert.
- Container-Basis auf Debian Trixie umgestellt.
- Docker-Healthcheck mit internem /healthz- und SQLite-Test ergänzt.
[en]
- Fixed the restart loop on existing installations through a dedicated permission init service while keeping the web application unprivileged.
- Switched the container base to Debian Trixie.
- Added a Docker health check with an internal /healthz and SQLite probe.
